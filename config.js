// After deploying the backend (e.g. to Render), paste its URL here.
// Example: "https://eob-era-extractor-backend.onrender.com"
// Leave as "" to use the same origin (only works when FastAPI serves the
// frontend directly, e.g. when running locally with `uvicorn main:app`).
window.APP_CONFIG = {
  API_BASE: "",
};
